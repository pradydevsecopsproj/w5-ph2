name: Terraform CD

on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: read
  id-token: write

env:
  TF_ENV: prod
  TF_VERSION: '1.15.5'
  AWS_REGION: ap-south-1

concurrency:
  group: terraform-${{ env.TF_ENV }}
  cancel-in-progress: false

jobs:
  terraform-apply:

    runs-on: ubuntu-latest

    environment:
      name: prod

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      # Terraform initialization,
      # plan and apply...